# Google OAuth 2.0 Desktop Client ID
# Create a Google Cloud OAuth client with Application type: Desktop app.
# The EXE uses loopback 127.0.0.1 + PKCE; no client secret is embedded.
GOOGLE_CLIENT_ID=YOUR_DESKTOP_CLIENT_ID.apps.googleusercontent.com

# v2.0.5 build marker
